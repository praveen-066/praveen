<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Praveen D — Aspiring SOC Analyst</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@400;600;700;900&family=Inter:wght@400;500;600;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg: #0A0A0E;
    --surface: #121218;
    --border: rgba(255,255,255,0.09);
    --text-primary: #F6F6F8;
    --text-secondary: #A6A6B3;
    --text-muted: #6E6E7C;
    --violet: #8C6CFF;
    --coral: #FF6B6B;
    --lime: #D4FF3F;
    --font-display: 'Unbounded', sans-serif;
    --font-body: 'Inter', sans-serif;
    --font-mono: 'Space Mono', monospace;
  }

  *{ margin:0; padding:0; box-sizing:border-box; }
  html{ scroll-behavior:smooth; }
  body{
    background:var(--bg); color:var(--text-primary); font-family:var(--font-body);
    line-height:1.6; overflow-x:hidden;
    background-image: radial-gradient(rgba(255,255,255,0.05) 1px, transparent 1px);
    background-size: 26px 26px;
  }
  a{ color:inherit; text-decoration:none; }
  ::selection{ background:var(--lime); color:#0A0A0E; }

  /* MARQUEE */
  .marquee-band{
    background:linear-gradient(90deg, var(--violet), var(--coral));
    padding:10px 0; overflow:hidden; white-space:nowrap;
    border-bottom:1px solid var(--border);
  }
  .marquee-track{ display:inline-flex; gap:40px; animation:scroll-left 26s linear infinite; }
  .marquee-track span{
    font-family:var(--font-mono); font-weight:700; font-size:12.5px; color:#0A0A0E;
    letter-spacing:0.5px; text-transform:uppercase;
  }
  @keyframes scroll-left{ from{ transform:translateX(0); } to{ transform:translateX(-50%); } }

  /* LAYOUT */
  .layout{
    max-width:1180px; margin:0 auto; padding:36px 28px 80px;
    display:grid; grid-template-columns:280px 1fr; gap:56px; align-items:start;
  }
  @media (max-width:900px){
    .layout{ grid-template-columns:1fr; gap:32px; padding:24px 20px 60px; }
  }

  /* SIDEBAR */
  .sidebar{
    position:sticky; top:28px; align-self:start;
    max-height:calc(100vh - 56px); overflow-y:auto;
    padding-right:6px;
  }
  .sidebar::-webkit-scrollbar{ width:4px; }
  .sidebar::-webkit-scrollbar-thumb{ background:var(--border); border-radius:4px; }
  @media (max-width:900px){
    .sidebar{ position:static; max-height:none; overflow:visible; padding-right:0; }
  }

  .sidebar-visual{ position:relative; margin-bottom:22px; }
  .sidebar-visual .blob{
    position:absolute; top:-40px; left:-40px; width:280px; height:280px;
    background:radial-gradient(circle, rgba(140,108,255,0.35), transparent 70%);
    filter:blur(18px); z-index:0; animation:blob-move 12s ease-in-out infinite; pointer-events:none;
  }
  @keyframes blob-move{
    0%,100%{ opacity:0.55; transform:translate(0,0) scale(1); }
    33%{ opacity:0.85; transform:translate(-14px,10px) scale(1.06); }
    66%{ opacity:0.7; transform:translate(8px,-8px) scale(0.96); }
  }
  .sidebar-visual svg{ position:relative; z-index:1; width:100%; max-width:220px; display:block; margin:0 auto; }
  .float-icon{ animation:float-y 3.4s ease-in-out infinite; }
  .float-icon.delay1{ animation-delay:0.6s; }
  .float-icon.delay2{ animation-delay:1.2s; }
  @keyframes float-y{ 0%,100%{ transform:translateY(0); } 50%{ transform:translateY(-8px); } }

  .sidebar-name{
    font-family:var(--font-display); font-weight:900; font-size:26px;
    letter-spacing:-0.5px; text-transform:uppercase; margin-bottom:6px; text-align:center;
  }
  .sidebar-role{
    background:linear-gradient(90deg, var(--violet), var(--coral), var(--lime), var(--violet));
    background-size:300% 100%; -webkit-background-clip:text; background-clip:text; color:transparent;
    animation:grad-shift 6s ease-in-out infinite;
    font-family:var(--font-display); font-weight:700; font-size:14px; text-align:center;
    text-transform:uppercase; letter-spacing:0.5px; margin-bottom:16px;
  }
  @keyframes grad-shift{ 0%{background-position:0% 50%;} 50%{background-position:100% 50%;} 100%{background-position:0% 50%;} }

  .sidebar-badges{ display:flex; gap:6px; flex-wrap:wrap; justify-content:center; margin-bottom:22px; }
  .sidebar-badges span{
    font-family:var(--font-mono); font-size:10.5px; color:var(--text-secondary);
    letter-spacing:0.5px; text-transform:uppercase; border:1px solid var(--border);
    padding:5px 11px; border-radius:20px;
  }

  .eyebrow{
    font-family:var(--font-mono); font-size:11px; color:var(--lime);
    letter-spacing:1.5px; text-transform:uppercase; margin:0 auto 22px; width:fit-content;
    display:inline-flex; align-items:center; gap:7px;
    border:1px solid rgba(212,255,63,0.35); padding:5px 12px; border-radius:30px;
    animation:badge-glow 2.4s ease-in-out infinite;
  }
  .eyebrow .dot{ width:6px; height:6px; border-radius:50%; background:var(--lime); box-shadow:0 0 8px var(--lime); }
  @keyframes badge-glow{ 0%,100%{ box-shadow:0 0 0 0 rgba(212,255,63,0.18); } 50%{ box-shadow:0 0 16px 2px rgba(212,255,63,0.22); } }

  .sidebar-eyebrow-wrap{ display:flex; justify-content:center; }

  nav.sidebar-nav{ display:flex; flex-direction:column; gap:4px; margin-bottom:24px; border-top:1px solid var(--border); padding-top:20px; }
  nav.sidebar-nav a{
    font-family:var(--font-mono); font-size:12.5px; color:var(--text-secondary);
    letter-spacing:0.5px; text-transform:uppercase; padding:9px 10px; border-radius:8px;
    position:relative; padding-left:20px; transition:color 0.2s ease, background 0.2s ease;
  }
  nav.sidebar-nav a::before{
    content:''; position:absolute; left:0; top:50%; transform:translateY(-50%);
    width:3px; height:0; background:var(--lime); border-radius:2px; transition:height 0.2s ease;
  }
  nav.sidebar-nav a:hover{ color:var(--text-primary); background:rgba(255,255,255,0.03); }
  nav.sidebar-nav a:hover::before{ height:16px; }

  .sidebar-cta{ display:flex; flex-direction:column; gap:10px; margin-bottom:22px; }
  .btn{
    font-family:var(--font-mono); font-weight:700; font-size:12.5px; letter-spacing:0.5px;
    padding:12px 20px; border-radius:40px; border:1px solid var(--border);
    transition:all 0.2s ease; text-transform:uppercase; text-align:center;
  }
  .btn-primary{ background:var(--lime); color:#0A0A0E; border-color:var(--lime); }
  .btn-primary:hover{ transform:translateY(-2px); box-shadow:0 8px 24px rgba(212,255,63,0.3); }
  .btn-outline{ color:var(--text-primary); }
  .btn-outline:hover{ border-color:var(--lime); color:var(--lime); transform:translateY(-2px); }

  .sidebar-footer{
    border-top:1px solid var(--border); padding-top:16px;
    font-family:var(--font-mono); font-size:11px; color:var(--text-muted); text-align:center; line-height:1.8;
  }

  /* MAIN */
  .main-content{ min-width:0; }
  section{ padding:0 0 56px; }
  section:not(:last-child){ border-bottom:1px solid var(--border); margin-bottom:56px; }
  .section-head{ display:flex; align-items:baseline; gap:12px; margin-bottom:28px; }
  .section-num{ font-family:var(--font-mono); font-size:12px; color:var(--lime); }
  h2{ font-family:var(--font-display); font-weight:700; font-size:clamp(20px,2.6vw,26px); letter-spacing:-0.4px; text-transform:uppercase; }

  .reveal{ opacity:0; transform:translateY(18px); transition:opacity 0.6s ease, transform 0.6s ease; }
  .reveal.in{ opacity:1; transform:translateY(0); }

  .intro-block{ margin-bottom:8px; }
  .intro-eyebrow{
    font-family:var(--font-mono); font-size:11.5px; color:var(--lime);
    letter-spacing:1.5px; text-transform:uppercase; margin-bottom:16px;
    display:inline-flex; align-items:center; gap:7px;
    border:1px solid rgba(212,255,63,0.35); padding:5px 13px; border-radius:30px;
    animation:badge-glow 2.4s ease-in-out infinite;
  }
  .intro-eyebrow .dot{ width:6px; height:6px; border-radius:50%; background:var(--lime); box-shadow:0 0 8px var(--lime); }
  .intro-block h1{
    font-family:var(--font-display); font-weight:900; font-size:clamp(28px,4vw,42px);
    letter-spacing:-1px; line-height:1.15; text-transform:uppercase; margin-bottom:16px;
  }
  .intro-block h1 .grad{
    background:linear-gradient(90deg, var(--violet), var(--coral), var(--lime), var(--violet));
    background-size:300% 100%; -webkit-background-clip:text; background-clip:text; color:transparent;
    animation:grad-shift 6s ease-in-out infinite;
  }
  .about-text{ font-size:16px; color:var(--text-secondary); max-width:640px; }
  .about-text strong{ color:var(--text-primary); font-weight:600; }
  .intro-cta{ display:flex; gap:12px; flex-wrap:wrap; margin-top:24px; }

  .stats-row{ display:flex; flex-wrap:wrap; gap:32px; margin-top:26px; }

  /* open to work */
  .openwork{
    border:1px solid rgba(212,255,63,0.3); border-radius:18px;
    background:linear-gradient(135deg, rgba(212,255,63,0.06), rgba(140,108,255,0.05));
    padding:28px 30px; position:relative; overflow:hidden;
  }
  .openwork::before{
    content:''; position:absolute; top:-60px; right:-60px; width:200px; height:200px;
    background:radial-gradient(circle, rgba(212,255,63,0.18), transparent 70%); pointer-events:none;
  }
  .openwork-head{ display:flex; align-items:center; gap:10px; margin-bottom:14px; }
  .openwork-head .pulse-dot{
    width:9px; height:9px; border-radius:50%; background:var(--lime); box-shadow:0 0 10px var(--lime);
    animation:badge-glow 2.4s ease-in-out infinite;
  }
  .openwork-head span{
    font-family:var(--font-mono); font-size:12px; color:var(--lime); letter-spacing:2px; text-transform:uppercase;
  }
  .openwork p{ font-size:15.5px; color:var(--text-secondary); max-width:600px; margin-bottom:20px; }
  .openwork p strong{ color:var(--text-primary); font-weight:600; }
  .role-row{ display:flex; flex-wrap:wrap; gap:9px; margin-bottom:22px; }
  .role-chip{
    font-family:var(--font-mono); font-size:12px; color:var(--text-primary);
    border:1px solid rgba(212,255,63,0.35); background:rgba(212,255,63,0.06);
    border-radius:20px; padding:8px 16px;
  }
  .openwork-cta{ display:flex; gap:12px; flex-wrap:wrap; }
  .stat{ display:flex; align-items:baseline; gap:9px; }
  .stat .num{ font-family:var(--font-display); font-weight:700; font-size:24px; color:var(--lime); }
  .stat .label{ font-family:var(--font-mono); font-size:10.5px; color:var(--text-muted); text-transform:uppercase; letter-spacing:0.5px; }

  /* skills */
  .skills-grid{ display:grid; grid-template-columns:repeat(2,1fr); gap:16px; }
  @media (max-width:640px){ .skills-grid{ grid-template-columns:1fr; } }
  .skill-card{
    border:1px solid var(--border); border-radius:14px; padding:20px;
    background:var(--surface); transition:border-color 0.2s ease, transform 0.2s ease;
  }
  .skill-card:hover{ border-color:var(--violet); transform:translateY(-3px); }
  .skill-card h3{ font-family:var(--font-display); font-size:12.5px; color:var(--lime); letter-spacing:0.5px; text-transform:uppercase; margin-bottom:12px; }
  .tag-row{ display:flex; flex-wrap:wrap; gap:7px; }
  .tag{ font-family:var(--font-mono); font-size:11.5px; color:var(--text-secondary); border:1px solid var(--border); border-radius:20px; padding:5px 12px; }

  /* projects */
  .project-card{
    border:1px solid var(--border); border-radius:16px;
    background:var(--surface); padding:26px; margin-bottom:16px;
    transition:transform 0.25s ease, border-color 0.25s ease; position:relative; overflow:hidden;
  }
  .project-card:hover{ transform:translateY(-4px) scale(1.008); border-color:var(--coral); }
  .project-card:hover::before{ opacity:1; }
  .project-card::before{
    content:''; position:absolute; inset:0; z-index:0; opacity:0; transition:opacity 0.3s ease;
    background:radial-gradient(circle at var(--mx,50%) var(--my,50%), rgba(140,108,255,0.22), transparent 55%);
  }
  .project-inner{ position:relative; z-index:1; }
  .project-head{ display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:10px; margin-bottom:9px; }
  .project-title{ font-family:var(--font-display); font-weight:600; font-size:17.5px; letter-spacing:-0.2px; }
  .badge{ font-family:var(--font-mono); font-size:10px; letter-spacing:1px; text-transform:uppercase; padding:4px 11px; border-radius:20px; font-weight:700; }
  .badge-major{ background:rgba(255,107,107,0.15); color:var(--coral); border:1px solid rgba(255,107,107,0.35); }
  .badge-mini{ background:rgba(212,255,63,0.15); color:var(--lime); border:1px solid rgba(212,255,63,0.35); }
  .project-stack{ font-family:var(--font-mono); font-size:11px; color:var(--text-muted); margin-bottom:12px; }
  .project-card p{ font-size:14px; color:var(--text-secondary); }

  /* education */
  .edu-row{ display:flex; justify-content:space-between; align-items:baseline; flex-wrap:wrap; gap:6px; padding:16px 0; border-bottom:1px solid var(--border); }
  .edu-row:last-child{ border-bottom:none; }
  .edu-left b{ font-family:var(--font-display); font-size:15px; font-weight:600; }
  .edu-left div{ font-size:13px; color:var(--text-secondary); margin-top:3px; }
  .edu-right{ font-family:var(--font-mono); font-size:12px; color:var(--text-muted); text-align:right; }
  .edu-right b{ color:var(--lime); display:block; }

  /* certifications */
  .cert-row{ display:flex; flex-wrap:wrap; gap:10px; }
  .cert-pill{
    font-family:var(--font-mono); font-size:12px; color:var(--text-primary);
    border:1px solid var(--border); background:var(--surface); border-radius:30px;
    padding:8px 16px; display:flex; align-items:center; gap:7px;
  }
  .cert-pill.progress{ border-color:rgba(212,255,63,0.4); }
  .cert-pill::before{ content:''; width:6px; height:6px; border-radius:50%; background:var(--lime); }

  #contact{ border-bottom:none; margin-bottom:0; padding-bottom:0; }
  .contact-links{ display:flex; gap:14px; flex-wrap:wrap; }
  .contact-links a{
    font-family:var(--font-mono); font-size:12.5px; color:var(--text-secondary);
    border:1px solid var(--border); padding:10px 18px; border-radius:30px; transition:all 0.2s ease;
  }
  .contact-links a:hover{ border-color:var(--lime); color:var(--lime); }

  @media (prefers-reduced-motion: reduce){ *{ animation:none !important; transition:none !important; } }
</style>
</head>
<body>

<div class="marquee-band">
  <div class="marquee-track">
    <span>NETWORK SECURITY</span><span>★</span><span>THREAT DETECTION</span><span>★</span>
    <span>INCIDENT RESPONSE</span><span>★</span><span>SIEM</span><span>★</span>
    <span>eBPF MONITORING</span><span>★</span><span>OPEN TO OPPORTUNITIES</span><span>★</span>
    <span>NETWORK SECURITY</span><span>★</span><span>THREAT DETECTION</span><span>★</span>
    <span>INCIDENT RESPONSE</span><span>★</span><span>SIEM</span><span>★</span>
    <span>eBPF MONITORING</span><span>★</span><span>OPEN TO OPPORTUNITIES</span><span>★</span>
  </div>
</div>

<div class="layout">

  <!-- SIDEBAR -->
  <aside class="sidebar">
    <div class="sidebar-visual">
      <div class="blob"></div>
      <svg viewBox="0 0 360 480" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <linearGradient id="bodyGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#8C6CFF"/><stop offset="100%" stop-color="#FF6B6B"/>
          </linearGradient>
          <linearGradient id="screenGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#D4FF3F"/><stop offset="100%" stop-color="#8C6CFF"/>
          </linearGradient>
        </defs>

        <!-- chair back -->
        <path d="M108 150 Q108 118 180 116 Q252 118 252 150 L258 320 Q180 336 102 320 Z" fill="#14141C" stroke="#232332" stroke-width="2"/>
        <!-- ground shadow -->
        <ellipse cx="180" cy="452" rx="100" ry="14" fill="#000000" opacity="0.35"/>
        <!-- seat -->
        <rect x="98" y="322" width="164" height="26" rx="12" fill="#181822"/>
        <!-- thighs -->
        <rect x="110" y="300" width="66" height="32" rx="16" fill="#181822"/>
        <rect x="184" y="300" width="66" height="32" rx="16" fill="#1E1E29"/>
        <!-- shins -->
        <rect x="118" y="326" width="32" height="94" rx="16" fill="#181822"/>
        <rect x="210" y="326" width="32" height="94" rx="16" fill="#1E1E29"/>
        <!-- shoes -->
        <rect x="108" y="416" width="52" height="18" rx="9" fill="#0A0A0E" stroke="#D4FF3F" stroke-width="2"/>
        <rect x="200" y="416" width="52" height="18" rx="9" fill="#0A0A0E" stroke="#D4FF3F" stroke-width="2"/>
        <!-- torso: hoodie -->
        <path d="M122 220 Q122 186 180 182 Q238 186 238 220 L244 302 Q180 318 116 302 Z" fill="url(#bodyGrad)"/>
        <path d="M150 264 Q180 278 210 264 L206 300 Q180 310 154 300 Z" fill="#00000022"/>
        <line x1="172" y1="202" x2="169" y2="232" stroke="#0A0A0E" stroke-width="3" stroke-linecap="round"/>
        <line x1="188" y1="202" x2="191" y2="232" stroke="#0A0A0E" stroke-width="3" stroke-linecap="round"/>
        <circle cx="169" cy="234" r="3" fill="#0A0A0E"/>
        <circle cx="191" cy="234" r="3" fill="#0A0A0E"/>
        <!-- back arm -->
        <rect x="120" y="212" width="28" height="88" rx="14" fill="#8C6CFF" transform="rotate(-10 134 212)"/>
        <!-- neck -->
        <rect x="164" y="158" width="32" height="28" rx="8" fill="#2A2A38"/>
        <!-- hood -->
        <path d="M126 158 Q124 104 180 100 Q236 104 234 158 Q220 126 180 124 Q140 126 126 158 Z" fill="#6C4FE0"/>
        <!-- head -->
        <circle cx="180" cy="130" r="44" fill="#20202B" stroke="#D4FF3F" stroke-width="2.5"/>
        <circle cx="165" cy="128" r="4.5" fill="#D4FF3F"/>
        <circle cx="195" cy="128" r="4.5" fill="#D4FF3F"/>
        <path d="M166 146 Q180 155 194 146" stroke="#D4FF3F" stroke-width="3" fill="none" stroke-linecap="round"/>
        <!-- headset -->
        <path d="M136 116 Q180 70 224 116" fill="none" stroke="#0A0A0E" stroke-width="7" stroke-linecap="round"/>
        <circle cx="136" cy="126" r="14" fill="#0A0A0E" stroke="#D4FF3F" stroke-width="2"/>
        <circle cx="224" cy="126" r="14" fill="#0A0A0E" stroke="#D4FF3F" stroke-width="2"/>
        <path d="M224 138 Q222 160 198 166" fill="none" stroke="#0A0A0E" stroke-width="4" stroke-linecap="round"/>
        <circle cx="197" cy="167" r="4.5" fill="#D4FF3F"/>
        <!-- front arm -->
        <rect x="212" y="212" width="28" height="86" rx="14" fill="#FF6B6B" transform="rotate(10 226 212)"/>
        <!-- laptop -->
        <rect x="126" y="296" width="108" height="14" rx="4" fill="#0A0A0E" stroke="#3A3A48" stroke-width="1.5"/>
        <rect x="130" y="224" width="100" height="76" rx="6" fill="#101018" stroke="#3A3A48" stroke-width="2" transform="rotate(-2 180 262)"/>
        <rect x="138" y="232" width="84" height="60" rx="3" fill="url(#screenGrad)" opacity="0.85" transform="rotate(-2 180 262)"/>
        <g transform="rotate(-2 180 262)">
          <rect x="168" y="258" width="24" height="20" rx="3" fill="#0A0A0E"/>
          <path d="M172 258 v-8 a8 8 0 0 1 16 0 v8" fill="none" stroke="#0A0A0E" stroke-width="4"/>
        </g>
        <!-- floating icons -->
        <g class="float-icon">
          <circle cx="66" cy="150" r="8" fill="none" stroke="#D4FF3F" stroke-width="2.2"/>
          <path d="M66 142 v-9 M66 167 v-9 M58 150 h-9 M74 150 h9" stroke="#D4FF3F" stroke-width="1.8" stroke-linecap="round"/>
        </g>
        <g class="float-icon delay1">
          <rect x="282" y="180" width="18" height="14" rx="3" fill="none" stroke="#FF6B6B" stroke-width="2.2"/>
          <path d="M286 180 v-5 a5 5 0 0 1 10 0 v5" fill="none" stroke="#FF6B6B" stroke-width="2.2"/>
        </g>
        <g class="float-icon delay2">
          <circle cx="292" cy="90" r="4.5" fill="#8C6CFF"/>
          <circle cx="308" cy="104" r="3" fill="#D4FF3F"/>
          <line x1="292" y1="90" x2="308" y2="104" stroke="#8C6CFF" stroke-width="1.4"/>
        </g>
      </svg>
    </div>

    <div class="sidebar-eyebrow-wrap">
      <div class="eyebrow"><span class="dot"></span>Available for work</div>
    </div>

    <div class="sidebar-name">Praveen D</div>
    <div class="sidebar-role">SOC Analyst</div>
    <div class="sidebar-badges">
      <span>Cybersecurity</span><span>&amp;</span><span>Networking</span>
    </div>

    <nav class="sidebar-nav">
      <a href="#about">About</a>
      <a href="#open-to-work">Open to Work</a>
      <a href="#skills">Skills</a>
      <a href="#projects">Projects</a>
      <a href="#education">Education</a>
      <a href="#certifications">Certifications</a>
      <a href="#contact">Contact</a>
    </nav>

    <div class="sidebar-cta">
      <a class="btn btn-primary" href="mailto:praveen.durgesh06@gmail.com">Get in touch →</a>
      <a class="btn btn-outline" href="#" id="linkedin-link">LinkedIn</a>
      <a class="btn btn-outline" href="#" id="github-link">GitHub</a>
    </div>

    <div class="sidebar-footer">
      Chitradurga, Karnataka, India<br>
      +91-7795143853<br>
      praveen.durgesh06@gmail.com
    </div>
  </aside>

  <!-- MAIN -->
  <main class="main-content">

    <section id="about">
      <div class="intro-block reveal">
        <div class="intro-eyebrow"><span class="dot"></span>Available for opportunities</div>
        <h1>Cybersecurity &amp; <span class="grad">Network Security</span></h1>
        <p class="about-text">
          I'm working toward a career as a <strong>SOC Analyst</strong> — final-year AI &amp; Data Science
          student with hands-on experience securing enterprise networks (VLANs, routing, ACLs, DNS/DHCP)
          and practical exposure to <strong>Wireshark</strong>, <strong>Nmap</strong>, and
          <strong>Kali Linux</strong>. Currently pursuing <strong>CompTIA Security+</strong>, and looking for
          an entry-level opportunity in security monitoring and incident response.
        </p>
        <div class="intro-cta">
          <a class="btn btn-primary" href="mailto:praveen.durgesh06@gmail.com">Get in touch →</a>
          <a class="btn btn-outline" href="#projects">View projects</a>
        </div>
        <div class="stats-row">
          <div class="stat"><span class="num" data-count-to="8.05" data-decimals="2">0</span><span class="label">CGPA</span></div>
          <div class="stat"><span class="num" data-count-to="5" data-decimals="0" data-pad="2">0</span><span class="label">Projects Built</span></div>
          <div class="stat"><span class="num" data-count-to="2027" data-decimals="0">0</span><span class="label">Graduating</span></div>
        </div>
      </div>
    </section>

    <section id="open-to-work">
      <div class="openwork reveal">
        <div class="openwork-head"><span class="pulse-dot"></span><span>Open to Work</span></div>
        <p>
          I'm ready to start contributing now — <strong>hands-on with network security, threat detection,
          and incident response</strong>, and comfortable with SIEM concepts, Wireshark, Nmap, and secure
          network configuration. Looking for an entry-level opportunity where I can put these skills to work
          from day one.
        </p>
        <div class="role-row">
          <span class="role-chip">SOC Analyst</span>
          <span class="role-chip">Cybersecurity Analyst</span>
          <span class="role-chip">Network Security Analyst</span>
          <span class="role-chip">IT Security Support</span>
        </div>
        <div class="openwork-cta">
          <a class="btn btn-primary" href="mailto:praveen.durgesh06@gmail.com">Hire me →</a>
          <a class="btn btn-outline" href="#projects">See my projects</a>
        </div>
      </div>
    </section>

    <section id="skills">
      <div class="section-head"><span class="section-num">01</span><h2>Technical Skills</h2></div>
      <div class="skills-grid">
        <div class="skill-card reveal">
          <h3>Security &amp; Networking</h3>
          <div class="tag-row">
            <span class="tag">TCP/IP</span><span class="tag">VLAN Config</span><span class="tag">Inter-VLAN Routing</span>
            <span class="tag">Static Routing</span><span class="tag">OSPF</span><span class="tag">DNS</span>
            <span class="tag">DHCP</span><span class="tag">VTP</span><span class="tag">ACLs</span>
            <span class="tag">Segmentation</span><span class="tag">SSH Spoofing (basics)</span>
          </div>
        </div>
        <div class="skill-card reveal">
          <h3>Security Tools</h3>
          <div class="tag-row">
            <span class="tag">Wireshark</span><span class="tag">Nmap</span><span class="tag">Kali Linux</span>
            <span class="tag">Cisco Packet Tracer</span>
          </div>
        </div>
        <div class="skill-card reveal">
          <h3>DevOps &amp; Security Monitoring</h3>
          <div class="tag-row">
            <span class="tag">Docker</span><span class="tag">Jenkins</span><span class="tag">Kubernetes</span>
            <span class="tag">eBPF (runtime monitoring)</span>
          </div>
        </div>
        <div class="skill-card reveal">
          <h3>Programming &amp; Analytics</h3>
          <div class="tag-row">
            <span class="tag">Python</span><span class="tag">C</span><span class="tag">Java (basic)</span>
            <span class="tag">SQL</span><span class="tag">Power BI</span><span class="tag">Excel</span>
          </div>
        </div>
      </div>
    </section>

    <section id="projects">
      <div class="section-head"><span class="section-num">02</span><h2>Projects</h2></div>

      <div class="project-card reveal">
        <div class="project-inner">
          <div class="project-head">
            <div class="project-title">CI/CD Pipeline for Full-Stack Web Application using eBPF</div>
            <span class="badge badge-major">Major Project</span>
          </div>
          <div class="project-stack">Docker · Jenkins · Kubernetes · eBPF</div>
          <p>Designed a DevOps pipeline for automated build, integration, and deployment of a full-stack web
          application. Integrated eBPF-based runtime security monitoring to detect anomalous system-call and
          process behavior — combining DevOps automation with real security observability.</p>
        </div>
      </div>

      <div class="project-card reveal">
        <div class="project-inner">
          <div class="project-head">
            <div class="project-title">CI/CD Pipeline for Full-Stack Web Application</div>
            <span class="badge badge-mini">Mini Project</span>
          </div>
          <div class="project-stack">Docker · Jenkins · Kubernetes</div>
          <p>Implemented a DevOps pipeline for automated build, integration, deployment, and monitoring —
          practical exposure to secure deployment and infrastructure automation.</p>
        </div>
      </div>

      <div class="project-card reveal">
        <div class="project-inner">
          <div class="project-head"><div class="project-title">Smart Campus Network</div></div>
          <div class="project-stack">Cisco Packet Tracer</div>
          <p>Designed a smart campus network with VLANs, inter-VLAN routing, DHCP, DNS, and web services —
          routers, switches, and servers configured for secure connectivity.</p>
        </div>
      </div>

      <div class="project-card reveal">
        <div class="project-inner">
          <div class="project-head"><div class="project-title">Enterprise Office Network</div></div>
          <div class="project-stack">Cisco Packet Tracer</div>
          <p>Built an enterprise office network with department VLANs, OSPF routing, NAT, ACLs, DHCP, and DNS
          services for secure, scalable connectivity.</p>
        </div>
      </div>

      <div class="project-card reveal">
        <div class="project-inner">
          <div class="project-head"><div class="project-title">Agri Connect — Farmer Marketplace Platform</div></div>
          <div class="project-stack">Web Application</div>
          <p>Developed a web platform connecting farmers directly with buyers to improve market access and
          reduce intermediaries.</p>
        </div>
      </div>
    </section>

    <section id="education">
      <div class="section-head"><span class="section-num">03</span><h2>Education</h2></div>
      <div class="reveal">
        <div class="edu-row">
          <div class="edu-left"><b>Shridevi Institute of Engineering and Technology, Tumkur</b><div>B.E. in Artificial Intelligence and Data Science</div></div>
          <div class="edu-right"><b>2023 – 2027</b>CGPA: 8.05</div>
        </div>
        <div class="edu-row">
          <div class="edu-left"><b>Morarji Desai Residential PU College, Chitradurga</b><div>2nd PUC</div></div>
          <div class="edu-right"><b>2023</b>78%</div>
        </div>
        <div class="edu-row">
          <div class="edu-left"><b>Morarji Desai Residential School, Chitradurga</b><div>SSLC</div></div>
          <div class="edu-right"><b>2021</b>89.12%</div>
        </div>
      </div>
    </section>

    <section id="certifications">
      <div class="section-head"><span class="section-num">04</span><h2>Certifications</h2></div>
      <div class="cert-row reveal">
        <div class="cert-pill progress">CompTIA Security+ — In Progress</div>
        <div class="cert-pill">Cybersecurity Workshops / Bootcamps</div>
        <div class="cert-pill">Power BI Workshop</div>
        <div class="cert-pill">Cisco / Networking Training</div>
      </div>
    </section>

    <section id="contact">
      <div class="section-head"><span class="section-num">05</span><h2>Contact</h2></div>
      <p class="about-text reveal" style="margin-bottom:22px;">
        Open to entry-level SOC Analyst and Cybersecurity Analyst roles — reach out any time.
      </p>
      <div class="contact-links reveal">
        <a href="mailto:praveen.durgesh06@gmail.com">praveen.durgesh06@gmail.com</a>
        <a href="tel:+917795143853">+91-7795143853</a>
        <a href="#" id="linkedin-link-2">LinkedIn</a>
        <a href="#" id="github-link-2">GitHub</a>
      </div>
    </section>

  </main>
</div>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => { if(e.isIntersecting){ e.target.classList.add('in'); } });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  document.querySelectorAll('#linkedin-link, #linkedin-link-2').forEach(a => a.href = 'https://www.linkedin.com/');
  document.querySelectorAll('#github-link, #github-link-2').forEach(a => a.href = 'https://github.com/');

  const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  function animateCount(el){
    const target = parseFloat(el.dataset.countTo);
    const decimals = parseInt(el.dataset.decimals || '0', 10);
    const pad = parseInt(el.dataset.pad || '0', 10);
    if(prefersReduced){
      el.textContent = pad ? String(target).padStart(pad,'0') : target.toFixed(decimals);
      return;
    }
    const duration = 1400;
    const start = performance.now();
    function tick(now){
      const progress = Math.min((now - start) / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      const value = target * eased;
      el.textContent = pad ? String(Math.round(value)).padStart(pad,'0') : value.toFixed(decimals);
      if(progress < 1){ requestAnimationFrame(tick); }
    }
    requestAnimationFrame(tick);
  }
  const statObserver = new IntersectionObserver((entries) => {
    entries.forEach(e => { if(e.isIntersecting){ animateCount(e.target); statObserver.unobserve(e.target); } });
  }, { threshold: 0.5 });
  document.querySelectorAll('.num[data-count-to]').forEach(el => statObserver.observe(el));

  if(!prefersReduced){
    document.querySelectorAll('.project-card').forEach(card => {
      card.addEventListener('mousemove', (e) => {
        const rect = card.getBoundingClientRect();
        const mx = ((e.clientX - rect.left) / rect.width) * 100;
        const my = ((e.clientY - rect.top) / rect.height) * 100;
        card.style.setProperty('--mx', mx + '%');
        card.style.setProperty('--my', my + '%');
      });
    });
  }
</script>

</body>
</html>
